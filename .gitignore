# dbt (package-lock.yml is committed so installs stay reproducible)
target/
dbt_packages/
logs/
.user.yml

# Python / uv (lockfile is committed; local env is not)
.venv/

# Environment / secrets
.env
.env.*
!.env.example

# OS
.DS_Store

# Logs
*.log
