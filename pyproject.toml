[project]
name = "cortexedge"
version = "1.0.0"
description = "Real-time financial NLP on Snowflake (dbt + Cortex)."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = [
    "dbt-snowflake>=1.8,<2",
]

[tool.uv]
package = false
